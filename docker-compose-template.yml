services:
  backend:
    build: ./backend
    environment:
      - TMDB_API_KEY=${TMDB_API_KEY}
      - PUID=1000
      - PGID=1000
    volumes:
      - /path/to/your/config:/config:ro
      - /path/to/your/data:/data:rw
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "5173:5173"